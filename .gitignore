## ------------------------------------------------------------------
## .NET / ASP.NET
## ------------------------------------------------------------------

# Carpetas de compilación (se regeneran con dotnet build/restore)
[Bb]in/
[Oo]bj/

# Archivos de usuario de Visual Studio / Rider
*.user
*.suo
.vs/
.idea/

# Resultados de pruebas y cobertura
[Tt]est[Rr]esult*/
*.trx
coverage/
*.coverage

# Publicación (dotnet publish)
[Pp]ublish/
*.pubxml
*.pubxml.user

# NuGet
*.nupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/
project.lock.json
project.fragment.lock.json
artifacts/

# ------------------------------------------------------------------
## Secretos y configuración local
## ------------------------------------------------------------------

# Configuración local con datos sensibles (connection strings reales, etc.)
appsettings.Development.json
appsettings.*.local.json
appsettings.Secrets.json

# User Secrets de .NET (normalmente viven fuera del repo, pero por si acaso)
secrets.json

# Variables de entorno locales
.env
.env.local
*.env

# ------------------------------------------------------------------
## Sistema operativo / editor
## ------------------------------------------------------------------

.DS_Store
Thumbs.db
*.swp

# ------------------------------------------------------------------
## Podman / contenedores
## ------------------------------------------------------------------

# Archivos de override locales que no quieres compartir con el equipo
docker-compose.override.yml
docker-compose.dev.override.yml

# ------------------------------------------------------------------
## NOTA: lo que SÍ se sube al repo (no lo agregues aquí):
##   - docker-compose.yml, docker-compose.dev.yml
##   - Containerfile, Containerfile.dev
##   - .devcontainer/devcontainer.json
##   - database/init/db_script.sql
##   - Tournament.sln, *.csproj, *.cs
## Todo esto es "infraestructura como código", no son secretos.
## ------------------------------------------------------------------
PRUEBAS.md
